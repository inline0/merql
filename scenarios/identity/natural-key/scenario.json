{
    "name": "natural-key",
    "category": "identity",
    "description": "Match rows by unique columns instead of PK",
    "tables": ["test_settings"],
    "expectations": {
        "conflicts": 0,
        "operations": 1,
        "is_clean": true
    }
}
